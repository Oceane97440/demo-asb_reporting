<%- include("../../layouts/manager/head.ejs") -%>
    <div class="col-sm-12" id="campaign-repartitions">

        <div class="card">
            <div class="card-header">
                <h1>Répartitions des instreams</h1>
            </div>
            <div class="card-body">
                <ul class="list-group list-group-flush"> 
                <% for (let i = 0; i < campaigns.length; i++) { %>
                    <li class="list-group-item"><%= campaigns[i].campaign_name %> - <%= campaigns[i].campaign_id %></li>
                <% } %>
                </ul>

                <hr />
                <% for (let i = 0; i < campaigns.length; i++) { %>
                   <%= campaigns[i].campaign_id %>,
                <% } %>

            </div>
        </div>

    </div>
    <%- include("../../layouts/manager/footer.ejs") -%>