<table class="table table-striped table_sort">
    <thead>
        <tr>
            <th>#</th>
            <th>Email</th>
            <th>Rôle</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        <% for (let i=0; i < users.length; i++) { %>           
            <tr>
                <td><a href="/manager/users/<%= users[i].user_id %>" title="<%= users[i].user_name %>"><%= users[i].user_id %></a></td> 
                <td><a href="/manager/users/<%= users[i].user_id %>" title="<%= users[i].user_name %>"><%= users[i].user_name %></a></td> 
                <td><%= users[i].role %></td>
                <td>                    
                   <a href="/manager/users/<%= users[i].user_id %>/edit" title="Modifier ce rôle"><i class="icon feather icon-edit f-16 text-success"></i></a>
                </td>
            </tr>
        <% } %>
        </tbody>
    </table>