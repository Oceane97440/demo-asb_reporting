<div class="table-responsive">
<table class="table table-striped table_sort">
    <thead>
        <tr>
            <th>#</th>
            <th>Nom</th>
            <th>Nb. Campagnes</th>
            <th>Archive</th>
            <th>Dernière MAJ</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        <% for (let i=0; i < agencies.length; i++) { %>
        <tr>
            <td><a href="/manager/agencies/<%= agencies[i].agency_id %>" title="<%= agencies[i].agency_name %>"><%= agencies[i].agency_id %></td>
            <td><a href="/manager/agencies/<%= agencies[i].agency_id %>" title="<%= agencies[i].agency_name %>"><%= agencies[i].agency_name %></td>
            <td><span class="badge rounded-pill bg-primary"><%= agencies[i].campaigns.length %></span></td>
            <td><% switch(agencies[i].agency_archived) { case 0: %> <span class="badge bg-warning">Archiv&eacute;</span> <% break; default: %> - <% break; }  %></td>
            <td><%= agencies[i].updated_at %></td>
            <td>               
                <a href="/manager/agencies/<%= agencies[i].agency_id %>/edit" title="Modifier cette campagne"><i class="icon feather icon-edit f-16 text-success"></i></a>
            </td>
        </tr>
        <% } %>
    </tbody>
</table>
</div>